# retromachina-frontend
=)
## Development

### Installation

* Clone this repo
* Install nodejs
    * Node Version Manager
  ```bash
  nvm use
  ```
    * *or install nodejs not older than presented in `.nvmrc`*
* `npm install`

# docker

```bash
    docker-compose up -d
```